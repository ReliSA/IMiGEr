/**
 * Class representing a coordinates pair.
 */
class Coordinates {
	/**
	 * @constructor
	 * @param {float} x X coordinate.
	 * @param {float} y Y coordinate.
	 */
	constructor(x, y) {
		this.x = x;
		this.y = y;
	}
}
